---
tags:
- 规范
- 工具库
- 多端同步
- 接入
created: 2026-08-27
status: ✅
permalink: main/工作/项目/资产/ai-工具库/各端同步接入指南
---

# 各端同步接入指南（新设备 / 新 AI 如何加入统一工具库）

> 配套：[[统一工具库接入指南]]（各端 AI 行为）、[[AI工具库/README]]、[[SESSIONS]]。
> 本文专门回答：**一个新端（台式机 Codex、Mac WorkBuddy、办公内网机等）要怎样"同步进来"**。

## 一、核心前提：加入同一个 verysync 同步文件夹（vault）
整个体系跨端工作的前提，是**每个端都挂着同一份 vault 同步文件夹**。工具库、会话索引、规范都在 vault 树内，verysync 负责把它们实时推到各端。
- 新设备装微力同步(verysync)，用你已有的 **vault 同步密钥/链接** 加入 → vault 自动落盘，目录结构与软路由一致。
- 这一步不需要 GitHub、不需要外网——verysync 局域网/自建中继优先，**即使工作机外网差也能同步**。

## 二、新端 AI 的开工动作（自动，靠规范）
vault 内 `00-规范/多端AI同步执行指令.md` 第 9 条已固化接入要求。新端 AI 开工读规范即自动：
1. 读 `AI工具库/统一工具库接入指南.md` + `README.md` + `协作协议.md`。
2. 列出 `AI工具库/` 下工具，建立「工具名→路径」索引。
3. 改造工具前查 `MANIFEST.json.lock` 与 `.sessions/` 跨端锁。

## 三、注册本端会话（让其他端看到你在线）
新端首次开工跑一次 beat（写自己的 `<endpoint>__<ai>.json`，**只写自己、只读别人**）：
```bash
python3 <本机vault绝对路径>/工作/项目/资产/AI工具库/.sessions/sessionctl.py \
  beat --endpoint <端名> --ai <AI名> --session <会话ID> --task "首次接入"
# 示例（台式机 Codex）：
python3 D:/Obsidian/zafkiel个人知识库/工作/项目/资产/AI工具库/.sessions/sessionctl.py beat --endpoint desktop --ai codex --session codex-1 --task "接入统一工具库"
```
跑完后在任意端 `view` 都能看到这条新会话。

## 四、双仓库（本地 git + 镜像）
工具库有两条版本历史通道，互不冲突：
- **仓库 A（主，日常）**：vault 内 `AI工具库/` 的本地 git，随 verysync 在各端落盘。各端在本地 `AI工具库/` 目录直接 `git` 操作即可（`.git` 已被 verysync 忽略，不会多端冲突）。
- **仓库 B（镜像备份，备）**：软路由 bare 仓库 `/workspace/ai-tool-lib.git`。用于跨机 pull/push 完整历史，不依赖外网（比 GitHub 更稳，适合外网差的工作机）。
  - 各端添加远程：`git remote add origin <软路由可达地址>/ai-tool-lib.git`
  - 软路由可达地址两种：①同机/局域网 `file:///workspace/ai-tool-lib.git`；②软路由起 HTTP git 服务后 `http://192.168.100.1:<port>/ai-tool-lib.git`（需软路由宿主配置，待办）。
- **GitHub 远程**：你工作机外网差，暂不强制；如需可另加 `git remote add github <url>`，但主同步仍以 verysync + 软路由镜像为准。

## 五、各端本地路径映射（必读，正视路径差异）
vault 在各端物理路径不同，AI 一律用**相对 vault 路径** `工作/项目/资产/AI工具库/`，运行时拼本机绝对路径：
| 端 | vault 绝对路径（示例） |
|---|---|
| 软路由（本容器） | `/workspace/同步文件/verysyncdown/zafkiel个人知识库` |
| 台式机 Windows | `D:\Obsidian\zafkiel个人知识库` 或 iCloud 路径 |
| MacBook | `~/Library/Mobile Documents/iCloud~md~obsidian/.../zafkiel个人知识库` |
| 办公内网机 | 微力同步落地目录 |

## 六、待办（需用户/宿主操作）
- 软路由宿主为 `/workspace/ai-tool-lib.git` 配 HTTP 访问（或确认局域网 file:// 可达），让工作机真正能 push/pull 镜像。
- 工作机/其他端首次开工 beat 注册会话，让 `.sessions/` 覆盖全部四端。
- GitHub 远程按需添加（外网差时非必选）。

## 关联
- [[统一工具库接入指南]]
- [[AI工具库/README]]
- [[SESSIONS]]
- [[协作协议]]