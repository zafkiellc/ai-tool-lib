---
name: 各端工具迁移提示词
type: note
permalink: main/工作/项目/资产/ai-工具库/各端工具迁移提示词
---

# 各端成品工具迁移提示词（复制发给各端 AI）

> 你是 zafkiel 统一 AI 工具库体系中的一个端。本库框架已搭好（结构 / 规范 / 双 git 远程 / 跨端锁见 `AI工具库/README.md` + `统一工具库接入指南.md`）。
> 现在请你**把自己本地已做出的成品工具 / 技能，迁移进统一库**，让各端都能继续优化。复制下面整段发给本端 AI 即可。

---

你是我（用户）的统一 AI 工具库体系中的一个端。请按以下步骤，把自己本地已做出的成品工具迁移进统一库，不要自行发挥：

## 第一步：接入（若尚未接入）
1. 确认本机已加入同一个 vault 同步密钥，vault 已落盘到本地路径 `<填你本机 vault 绝对路径，如 E:/zafkiel个人知识库 或 /Users/xxx/vault 或 D:/Apps/Verysync/zafkiel个人知识库>`。
2. 读规范：`00-规范/笔记工作流规范.md` + `00-总览/首页.md` + `AI工具库/README.md` + `AI工具库/协作协议.md` + `AI工具库/统一工具库接入指南.md`。
3. 注册会话（让其他端看到你在线）：
   ```bash
   python3 <vault>/工作/项目/资产/AI工具库/.sessions/sessionctl.py \
     beat --endpoint <端名> --ai <AI名> --session <会话ID> --task "迁移本地工具"
   ```

## 第二步：盘点本机成品工具
扫描你本机 / 本端所有「可复用的成品工具 / 技能」：
- WorkBuddy：`~/.workbuddy/skills/`（Windows 可能在 `C:\Users\<你>\AppData\...` 或 `D:\Apps\...`）
- Codex：`~/.codex/` 相关 skills / 本地脚本目录
- DSH：你容器内的自研脚本目录（如 `/workspace/...` 下）
- vault 内：检查 `私人/开发`、`私人/游戏`、`工作/项目/` 下是否有「应用 / 小工具」类笔记尚未在 `AI工具库/` 内
- **避免重复**：`AI工具库/` 已有 9 个工具（fss-sinopec-query / 液位仪系统数据查询 / sinopec-loss-analysis / compress-pdf-folder / organize-personal-folder / sap-zhqr023-loss-pipeline / 网络学院学习小助手 / 现场检查条目智能匹配工具 / wechat-cli本地查询），不要重建已有的。

对每件，判断归类：
- **跨端可复用**（脚本 / skill / 应用，别人也能用）→ 必须迁入 `AI工具库/`
- **私人向小工具**（仅自己用，如微信答题 / 三角洲改枪码）→ 保持原位，加 `[[AI工具库]]` 双链即可，不进库

## 第三步：迁入（跨端可复用的）
对每个要迁入的工具：
1. 复制 `AI工具库/_template/` 为 `AI工具库/<工具名>/`
2. **把实际代码复制进 `scripts/`**（关键！不要只写文档——源码不进库就达不到「各端都能优化」的目标）
3. 写 `SKILL.md`：Overview / 用法 / 依赖 / 注意事项；若代码暂无法复制进库，在 SKILL.md 写清「⚠️ 源码位置：<本机路径>」
4. 写 `MANIFEST.json`：`created_by` 写清是哪个端 / AI / 会话，`status` 设 `beta` 或 `stable`，`lock` 置 null，`entrypoint` 指向 `scripts/<主脚本>`
5. 原位置若留有笔记，补 `[[AI工具库]]` 双链指向新家（或删除避免重复——按「同主题不重复建档」原则）
6. **升级已有文档入口**：若工具是 `AI工具库/` 里已有的 3 个「文档入口」（网络学院学习小助手 / 现场检查条目智能匹配工具 / wechat-cli本地查询），且你本机有源码 → 直接补全它们的 `scripts/`，升级为完整工具（不必新建）

## 第四步：认领锁 + 提交（严守边界）
1. 迁入前：`sessionctl.py who-has --tool <名>` 查跨端锁；未被锁才继续，被锁则停下等释放
2. `claim` 锁（与 `MANIFEST.json.lock` 双写）
3. 只 `git add <你迁入的工具目录>/`（**不要 `git add .` / `git add -A`**，会误带别端改动）
4. `git commit -m "add <工具>: <说明>"`
5. `git push gitea master`（国内 / 局域网 `http://192.168.100.1:3000/zafkiel/ai-tool-lib.git` 或公网 `https://git.zafkiel.com.cn/zafkiel/ai-tool-lib.git`；gitea 凭据见 vault `私人/基础设施/家庭反代隧道密钥存档/gitea_admin.b64` 还原后读取，**不硬编码**）
6. 有 GitHub PAT 时（vault `github_token.b64`）`git push github master`；无则只走 gitea
7. `release` 锁
8. **绝不替其他端 commit / push**（详见 `统一工具库接入指南.md` 第 6 节 push 边界）

## 第五步：回报结果（给 zafkiel / Hermes 确认）
用一段结构化文字回报：
```
迁移端：<端名> / <AI名>
本机扫描到的成品工具：<清单>
已迁入 AI工具库：<工具名 → AI工具库/<工具名>/，含 scripts 与否>
保持原位 + 双链：<私人向清单>
跳过 / 未动：<原因，如代码不在本机、属其他端>
git commit：<hash>   push：<gitea/github 成功与否>
遗留问题：<如有>
```
回报后由路由端（Hermes）统一查看确认，无需你自行跨端核对。
