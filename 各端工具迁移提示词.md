---
name: 各端工具迁移提示词
type: note
permalink: main/工作/项目/资产/ai-工具库/各端工具迁移提示词
---

# 各端成品工具迁移提示词（复制发给各端 AI）

> 你是 zafkiel 统一 AI 工具库体系中的一个端。本库框架已搭好（结构 / 规范 / 双 git 远程 / 跨端锁见 `AI工具库/README.md` + `统一工具库接入指南.md`）。
> **核心目标**：把你在本机做好的「成品工具项目目录」**整个物理搬进** `AI工具库/<工具名>/`，让所有端靠 verysync + git 双通道同步、随时在哪台机器都能直接运行。复制下面整段发给本端 AI 即可。

---

你是我（用户）的统一 AI 工具库体系中的一个端。请把**你自己本机做好的成品工具项目目录**，物理搬迁进统一库。重点：**搬的是整个可运行的项目目录（源码 + 启动脚本 + 依赖说明），不是只建一个文档壳**。

## 第一步：接入（若尚未接入）
1. 确认本机已加入同一个 vault 同步密钥，vault 已落盘到本地路径 `<填你本机 vault 绝对路径，如 E:/zafkiel个人知识库 或 /Users/xxx/vault 或 D:/Apps/Verysync/zafkiel个人知识库>`。
2. 读规范：`00-规范/笔记工作流规范.md` + `00-总览/首页.md` + `AI工具库/README.md` + `AI工具库/协作协议.md` + `AI工具库/统一工具库接入指南.md`。
3. 注册会话：
   ```bash
   python3 <vault>/工作/项目/资产/AI工具库/.sessions/sessionctl.py \
     beat --endpoint <端名> --ai <AI名> --session <会话ID> --task "迁移本地工具"
   ```

## 第二步：找到你本机的成品工具项目目录（关键！）
**源码通常不在 AI 的 skills 目录，而在你手动放的项目目录**。逐端去这些真实位置找：
- **家端 Windows**：`C:\Users\zafki\OneDrive\工作\工作工具\`（如网络学院学习小助手）、`D:/巡站724_便携版/`（现场检查条目智能匹配工具）、`D:\Apps\...` 下你自建的项目
- **Mac**：`~/.local/venvs/`（如 wechat-cli）、`~/项目/`、`~/Developer/` 下你自建项目
- **WorkBuddy 端**：`~/.workbuddy/skills/`（你自建的 skill 项目，非官方）
- **DSH（deepseek）**：你容器内的自研脚本目录
- **vault 内**：`私人/开发`、`私人/游戏`、`工作/项目/` 下若有「应用 / 小工具」类**笔记+配套代码**，看笔记里写的"位置"字段指向哪个本机目录
- **避免重复**：`AI工具库/` 已有 9 个工具（fss-sinopec-query / 液位仪系统数据查询 / sinopec-loss-analysis / compress-pdf-folder / organize-personal-folder / sap-zhqr023-loss-pipeline / 网络学院学习小助手 / 现场检查条目智能匹配工具 / wechat-cli本地查询）。若库里已有该工具的**文档入口**（SKILL.md 但无 scripts），你的任务就是**把源码目录补进它的 `scripts/`**，升级为完整工具，不要新建重复目录。

对每件，判断归类：
- **跨端可复用**（脚本 / skill / 应用，别人也能跑）→ 必须整目录迁入 `AI工具库/`
- **私人向小工具**（仅自己用，如微信答题 / 三角洲改枪码）→ 保持原位，加 `[[AI工具库]]` 双链即可，不进库

## 第三步：整目录物理搬迁（核心！）
对每个要迁入的工具：
1. **找到该工具的完整项目目录**（含 `server.py` / `start.bat` / `package.json` / `scripts/` / 源码等）。
2. 在 `AI工具库/` 下建 `<工具名>/`，**把整个项目目录的内容复制进去**（不是只写 SKILL.md）：
   - 源码 → `scripts/` 或直接在 `<工具名>/` 下保持原结构
   - 启动脚本（start.bat / server.py / package.json）→ 留在原相对位置
   - 依赖说明 → 写进 `SKILL.md` 的「依赖」节或 `requirements.txt`/`package.json`
3. 写/补全 `SKILL.md`：Overview / 用法（**怎么启动、从哪个文件启动**）/ 依赖 / 注意事项；若某项依赖本机特有路径（如 `D:/巡站724_便携版/`），改成相对库内路径或写清"需在库内此目录运行"。
4. 写 `MANIFEST.json`：`created_by` 写清端/AI/会话，`status` 设 `beta` 或 `stable`，`lock` 置 null，`entrypoint` 指向库内实际启动文件。
5. **删掉原位置的项目目录**（或确认已迁空），避免两份并存不同步；原笔记若只是指向说明，补 `[[AI工具库]]` 双链或删除。
6. **验证可运行**：在 `AI工具库/<工具名>/` 目录内尝试启动/跑一下（如 `py_compile`、`node --check`、或实际起服务），确认搬过来后能跑，再提交。

## 第四步：认领锁 + 提交（严守边界）
1. 迁入前：`sessionctl.py who-has --tool <名>` 查跨端锁；未被锁才继续，被锁则停下等释放。
2. `claim` 锁（与 `MANIFEST.json.lock` 双写）。
3. 只 `git add <你迁入的工具目录>/`（**不要 `git add .` / `git add -A`**）。
4. `git commit -m "add <工具>: <说明>"`。
5. `git push gitea master`（国内 / 局域网 `http://192.168.100.1:3000/zafkiel/ai-tool-lib.git` 或公网 `https://git.zafkiel.com.cn/zafkiel/ai-tool-lib.git`；gitea 凭据见 vault `私人/基础设施/家庭反代隧道密钥存档/gitea_admin.b64` 还原后读取，**不硬编码**）。
6. 有 GitHub PAT 时（vault `github_token.b64`）`git push github master`；无则只走 gitea。
7. `release` 锁。
8. **绝不替其他端 commit / push**（详见 `统一工具库接入指南.md` 第 6 节 push 边界）。

## 第五步：回报结果（写进回报目录，路由端确认）
把回报写成 `AI工具库/迁移回报/<端名>__<AI名>.md`（格式见 `迁移回报/README.md`）：
```
迁移端：<端名> / <AI名>
本机找到的成品工具项目目录：<清单 + 各自本机路径>
已整目录迁入 AI工具库：<工具名 → AI工具库/<工具名>/，含源码：是，启动文件：<路径>，已验证可运行：是/否>
保持原位 + 双链：<私人向清单>
跳过 / 未动：<原因，如代码不在本机、属其他端>
git commit：<hash>   push：<gitea/github 成功与否>
遗留问题：<如有，如某工具依赖本机绝对路径未改>
```
回报后路由端（Hermes）统一查看 `迁移回报/`、更新 `迁移状态看板.md` 并确认。

## 常见误区（务必避开）
- ❌ "本机 ~/.codex/skills 没有用户工具，所以无工具可迁" → 用户的成品工具**不在 AI skills 目录**，去 OneDrive / D盘 / Mac venv / WorkBuddy skills / vault 笔记里写的"位置"字段找。
- ❌ 只建 SKILL.md 文档壳、源码留本机 → 这达不到"各端都能直接用"的目标，必须整目录搬进来。
- ❌ `git add .` 把别端改动一起提交 → 只 add 你迁入的工具目录。
