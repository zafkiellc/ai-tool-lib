---
tags:
- 规范
- 工具库
- 多端接入
- 跨AI
- 同步
created: 2026-08-27
status: ✅
permalink: main/工作/项目/资产/ai-工具库/统一工具库接入指南
---

# 统一工具库接入指南（各端 AI 必读 + 新端同步）

> 本指南是**各端 AI 发现、加载、协同改造统一工具库的唯一入口**。
> 任何端（软路由 Hermes/DSH、台式机 Codex/WorkBuddy、MacBook、办公内网机）的 AI 接入 vault 后，**开工即读本文件并执行**。

## 一、工具库在哪
- **相对 vault 路径（跨端通用）**：`工作/项目/资产/AI工具库/`
- 所有 AI 工具成品的唯一收口目录，随微力同步(verysync) 各端实时一致。

## 二、各端本地路径映射（关键：正视各端路径不同）
AI 一律用**相对 vault 路径** + 运行时解析本机绝对路径，绝不硬编码某端路径。

| 端 / 设备 | vault 本地绝对路径（示例） |
|---|---|
| 软路由（本容器） | `/workspace/同步文件/verysyncdown/zafkiel个人知识库` |
| 台式机 Windows | `D:\Obsidian\zafkiel个人知识库` 或 iCloud 路径 |
| MacBook | `~/Library/Mobile Documents/iCloud~md~obsidian/.../zafkiel个人知识库` |
| 办公内网机 | 微力同步落地目录 |

**规则**：先探测本机 vault 根（读 `00-规范/笔记工作流规范.md` 所在目录上溯），再拼出工具库绝对路径。

## 三、新端如何"同步进来"（核心前提）
整个体系跨端工作的前提是**每个端都挂着同一份 vault 同步文件夹**：
1. 新设备装微力同步(verysync)，用你已有的 **vault 同步密钥/链接** 加入 → vault 自动落盘，目录结构与软路由一致。
2. 这一步**不需要 GitHub、不需要外网**——verysync 局域网/自建中继优先，工作机外网差也能同步。
3. vault 内 `00-规范/多端AI同步执行指令.md` 第 9 条已固化接入要求，AI 开工读规范即自动接入。

## 四、开工统一动作（每端 AI 都做）
1. 读 `00-规范/笔记工作流规范.md` + `00-总览/首页.md`（既有约定）。
2. 读 `AI工具库/README.md` + `AI工具库/协作协议.md` + 本指南。
3. 列出 `AI工具库/` 下所有工具目录，建立「工具名 → 绝对路径」索引。
4. 被要求用某工具时：先查跨端锁（见下）再动手。

## 五、跨端协同：会话锁（防并发覆盖）
每端 AI 只写自己的 `.sessions/<endpoint>__<ai>.json`，只读别人的 → verysync 同步、不冲突。
- 协调 CLI：`AI工具库/.sessions/sessionctl.py`（beat / view / who-has / claim / release），各端统一调用。
- **改造工具前先 `who-has --tool <名>` 查跨端锁**：被锁则停下等释放；`claim`/`release` 与 `MANIFEST.json.lock` 双写双清（详见 [[协作协议]]）。
- 协调文件由 verysync 同步、**不进 git**（`.gitignore` 排除易变 `*.json`）；规范 `SESSIONS.md` + `sessionctl.py` 入 git 可追溯。
- 注册本端会话（让其他端看到你在线）：
  ```bash
  python3 <本机vault绝对路径>/工作/项目/资产/AI工具库/.sessions/sessionctl.py \
    beat --endpoint <端名> --ai <AI名> --session <会话ID> --task "首次接入"
  # 示例（台式机 Codex）：
  python3 D:/Obsidian/zafkiel个人知识库/工作/项目/资产/AI工具库/.sessions/sessionctl.py beat --endpoint desktop --ai codex --session codex-1 --task "接入统一工具库"
  ```

## 六、git 双远程（已建，实测可用）
工具库有两条版本历史通道，互不冲突：
- **gitea（国内端）**：`http://192.168.100.1:3000/zafkiel/ai-tool-lib.git`（局域网）或 `https://git.zafkiel.com.cn/zafkiel/ai-tool-lib.git`（跨地反代，已通）。
- **github（外网好时）**：`https://github.com/zafkiellc/ai-tool-lib.git`（注意 owner 是 **zafkiellc**，非 zafkiel）。
- 本地已配 remote：`gitea` / `gitea-pub` / `github`（另有 `origin` 为本机 bare 备）。
- 提交格式：`<动作> <工具>: <一句话>`（add/lock/update/release/fix/docs）。`.git` 已被 verysync 忽略，不会多端冲突。

### ⚠️ push 边界（重要，避免跨端覆盖）
- **只 commit/push 你自己本次会话改造的工具**。看到工作区有**非本人**的未提交/未 push 改动（来自其他端 AI 或本人别的会话）——**不要替别人 commit 或 push**，只通过 `.sessions/who-has` 确认锁状态，或提示用户"有 N 个改动疑似来自其他端，是否需要我帮你推送"。
- **没有改造就不 push**：beat 注册会话不等于要 push（`.sessions/*.json` 已被 gitignore 排除，不进 git）。
- **凭据**：gitea push 用 `zafkiel` + 初始密码 `GiteaInit@2026`（仅局域网/公网 Gitea，上线后改密码）；github push 需你自己的 PAT（从 vault `github_token.b64` 取，**不要硬编码进工具库**，没有 PAT 就只走 gitea）。
- 示例（仅在你自己改造后）：
  ```bash
  git add <你改的工具目录> && git commit -m "update <工具>: <说明>"
  git push gitea master          # 国内端
  git push github master         # 外网好且你持有 PAT 时
  ```

## 七、关联
- [[AI工具库/README]]
- [[AI工具库/协作协议]]
- [[SESSIONS]]
- [[笔记工作流规范]]
- [[多端AI同步执行指令]]
- [[Gitea软路由部署]]（国内端 git 远程部署细节）
- [[新端接入提示词]]（直接复制发给新 AI 的整段提示词）
