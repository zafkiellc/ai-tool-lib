---
tags:
- 规范
- 工具库
- 多端接入
- 跨AI
created: 2026-08-27
status: ✅
permalink: main/工作/项目/资产/ai-工具库/统一工具库接入指南
---

# 统一工具库接入指南（各端 AI 必读）

> 本指南是**各端 AI 自动发现、加载、协同改造统一工具的唯一入口**。
> 任何端（台式机 Codex/WorkBuddy、MacBook、办公内网、软路由 Hermes/DSH）的 AI 接入本 vault 后，**开工即应读取本文件并执行**。

## 一、统一工具库在哪
- **相对 vault 路径（跨端通用）**：`工作/项目/资产/AI工具库/`
- 这是所有 AI 工具成品的**唯一收口目录**。工具随微力同步(verysync) + Fast Note Sync 在各端实时一致。

## 二、各端本地路径映射（关键：正视各端本地指向不同）
vault 在各端的物理路径不同，AI 必须把「相对 vault 路径」映射成本机绝对路径再读写：

| 端 / 设备 | vault 本地绝对路径（示例） | 工具库绝对路径 |
|---|---|---|
| 软路由（本容器） | `/workspace/同步文件/verysyncdown/zafkiel个人知识库` | `…/工作/项目/资产/AI工具库/` |
| 台式机 Windows | `D:\Obsidian\zafkiel个人知识库` 或 iCloud 路径 | `…\工作\项目\资产\AI工具库\` |
| MacBook | `~/Library/Mobile Documents/iCloud~md~obsidian/…/zafkiel个人知识库` | `…/工作/项目/资产/AI工具库/` |
| 办公内网机 | 微力同步落地目录 | `…/工作/项目/资产/AI工具库/` |

**规则**：AI 不要硬编码某端路径。先探测本机 vault 根（读 `00-规范/笔记工作流规范.md` 所在目录的上溯），再拼出工具库绝对路径。
代码里引用工具脚本时，也用「相对 vault 路径」或在运行时解析，避免跨端失效。

## 三、开工统一动作（每端 AI 都做）
1. 读 `00-规范/笔记工作流规范.md` + `00-总览/首页.md`（既有约定）。
2. 读 `AI工具库/README.md` + `AI工具库/协作协议.md` + 本指南。
3. 列出 `AI工具库/` 下所有工具目录，建立「工具名 → 绝对路径」索引。
4. 被要求用某工具时：先 `read` 该工具 `MANIFEST.json` 的 `lock` 字段（见下「认领锁」）。

## 四、多 AI 协同：认领锁（防并发覆盖）
每个工具目录 `MANIFEST.json` 含 `lock`：
```json
"lock": { "owner": "hermes:session-xxxx", "since": "ISO时间", "intent": "要改啥" }
```
- 改工具前：若 `lock.owner` 非空且不是自己 → **停下，不要改**，询问用户或等锁释放。
- 开始改：写入自己的 `owner/since/intent`，`git commit`（"lock <工具>: …"）。
- 改完：置 `lock: null`、`version` 递增、追加 `changelog`，`git commit`（"release <工具> vX: …"）。
- 锁尽量短时持有。详见 `协作协议.md`。

## 五、会话统一的设计（正视各端会话逻辑差异）
> 用户目标：不管在哪端，都能看到**统一的工具、skill、甚至工作区**；会话统一是演进方向，但需正视两端差异。

**已统一的部分（现在就生效）**：
- 工具/skill 实体：全部在 `AI工具库/`，verysync 保证各端文件一致。
- 记忆/笔记：vault 即记忆源，各端笔记互通（既有）。
- 接入协议：本指南 + 协作协议让各端 AI 行为一致。

**会话统一待解的真实问题（写入设计，待逐步落地）**：
1. **本地路径指向不同**：各端 vault 绝对路径不同 → 用「相对 vault 路径」+ 运行时解析（见第二节）。
2. **各端会话逻辑不同**：
   - Hermes/WorkBuddy：有 session、skills、cron、memory 体系。
   - Codex/DSH：另一套会话/上下文模型。
   - 统一会话 = 各端把「当前在做什么任务、锁了哪个工具」写入**共享会话索引** `AI工具库/.sessions/`，每端一个 `<endpoint>__<ai>.json` 文件，开工先扫描所有在线会话与跨端锁，避免两个 AI 同时动同一工具。**该机制已于 2026-08-27 建成**（见 [[SESSIONS]]）。
3. **凭据/环境差异**：工具脚本依赖的内网地址、token 各端可能不同 → 凭据绝不硬编码（既有护栏），运行时由用户现场提供或读本地 env。

**已落地的会话统一机制（`.sessions/`）**：
- 每端 AI 只写自己的 `<endpoint>__<ai>.json`，只读别人的 → verysync 同步、不冲突。
- 协调 CLI：`AI工具库/.sessions/sessionctl.py`（beat / view / who-has / claim / release），各端 AI 统一调用。
- 改造工具前先 `who-has --tool <名>` 查跨端锁；被锁则停下等释放；`claim`/`release` 与 `MANIFEST.json.lock` 双写双清。
- 协调文件由 verysync 同步、**不进 git**（`.gitignore` 排除易变 `*.json`）；规范 `SESSIONS.md` 与 `sessionctl.py` 入 git 可追溯。

**建议的下一步（待用户确认）**：
- 评估把 Hermes 框架 skill 中"用户自建"的也索引进库（框架层不复制，只建 `SKILL.md` 索引指向原路径）。

## 六、git 约定（本地，暂不远程）
- 仓库范围：`AI工具库/`（`.git` 已在 `.verysync/ignoreList` 忽略，不会多端冲突）。
- 提交格式：`<动作> <工具>: <一句话>`（add/lock/update/release/fix/docs）。
- 远程镜像（GitHub/Gitea）**暂未接**，待用户决定。

## 七、关联
- [[AI工具库/README]]
- [[AI工具库/协作协议]]
- [[笔记工作流规范]]
- [[多端AI同步执行指令]]