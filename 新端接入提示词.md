---
tags:
- 规范
- 工具库
- 多端接入
- 提示词
created: 2026-08-27
status: ✅
permalink: main/工作/项目/资产/ai-工具库/新端接入提示词
---

# 新端接入提示词（直接复制发给新 AI）

> 用途：台式机 Codex / Mac WorkBuddy / 任何其他新 AI 端首次开工时，把下面整段提示词发给它，它会自动接入统一工具库并注册跨端会话。
> 配套：[[统一工具库接入指南]]、[[协作协议]]、[[SESSIONS]]。

---

## 复制以下整段发给新 AI

```
你是我（用户）的统一 AI 工具库体系中的一个新端。请按以下步骤接入，不要自行发挥：

1. 我的 Obsidian 个人知识库（vault）通过微力同步(verysync)多端共享。请先确认你本机已加入同一个 vault 同步密钥，vault 已落盘到本地路径 <填你本机vault绝对路径，如 /Users/xxx/vault 或 D:/Apps/Verysync/zafkiel个人知识库>。

2. 开工先读规范（按规范第9条执行）：
   - 读 <vault>/00-规范/多端AI同步执行指令.md
   - 读 <vault>/00-总览/首页.md
   - 读 <vault>/工作/项目/资产/AI工具库/统一工具库接入指南.md
   - 读 <vault>/工作/项目/资产/AI工具库/各端同步接入指南.md

3. 工具库就在 <vault>/工作/项目/资产/AI工具库/。里面有 6 个可复用工具 + README.md（用法）+ 协作协议.md（改造规则）+ _template/SKILL.md（新工具模板）。要用哪个工具直接读对应目录的 README。

4. 注册你这个端到跨端会话索引（必须，避免和其他 AI 改同一个工具冲突）：
   在终端执行（把 <端名> 和 <AI名> 换成实际，如 desktop/codex、mac/workbuddy）：
   cd <vault>/工作/项目/资产/AI工具库
   python3 .sessions/sessionctl.py beat --endpoint <端名> --ai <AI名> --session <本次会话id> --task "<当前在做什么>"
   改造某个工具前先查锁：
   python3 .sessions/sessionctl.py who-has --tool <工具名>

5. 改造工具后提交到双 git 远程（仅限你本次会话自己改造的工具）：
   cd <vault>/工作/项目/资产/AI工具库
   git add <你实际改的工具目录> && git commit -m "<改动说明>"
   git push gitea master        # 国内/局域网 192.168.100.1:3000 或公网 git.zafkiel.com.cn
   git push github master       # 仅当你持有 GitHub PAT 时（github.com/zafkiellc/ai-tool-lib，owner 是 zafkiellc）
   ⚠️ 边界：不要替其他端/其他会话 commit 或 push 工作区里非你改动的未提交内容；没有改造就不 push；GitHub PAT 从 vault github_token.b64 取，不要硬编码进工具库。

6. 不要删库、不要批量移动目录、新规则入工具前先问我。敏感凭证不写进工具库。

读完后回我一句：已接入，端名=<端名>/<AI名>，当前在线会话有哪些。
```

---

## 占位符说明（发之前替成实际值，或让 AI 自己填）
- `<vault>`：本机 vault 绝对路径
- `<端名>` / `<AI名>`：如 desktop/codex、mac/workbuddy
- `<会话id>`：本次会话标识

## 机制要点
- 第 4 步 `beat` 写 `<vault>/.../.sessions/<endpoint>__<ai>.json`，各端只写自己的、只读别人的，verysync 同步不冲突。
- 第 5 步 `git push gitea` 前各端需配 git 凭证：局域网 Gitea 用 `zafkiel` + 初始密码 `GiteaInit@2026`；公网 `git.zafkiel.com.cn` 同账号。
- GitHub owner 是 **zafkiellc**（不是 zafkiel），见 [[AI工具库统一收口]] 扩展7。