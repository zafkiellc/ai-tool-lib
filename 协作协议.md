---
title: 协作协议
type: note
permalink: main/工作/项目/资产/ai-工具库/协作协议
---

# AI 工具库协作协议（多 AI 持续改造）

> 目标：让多个 AI（Hermes / WorkBuddy / DSH / Codex…）能**并发地对同一批工具进行改造优化而不互相覆盖**。

## 1. 认领锁（避免两人同时改一个工具）
每个工具目录下的 `MANIFEST.json` 含有 `lock` 字段：

```json
"lock": {
  "owner": "hermes:session-a6dbafdf7285",   // 谁在改
  "since": "2026-08-27T19:10:00+08:00",     // 认领时间
  "intent": "新增批量导出接口"               // 要做什么
}
```

规则：
- **改工具前**：先看 `MANIFEST.json` 的 `lock`。若 `owner` 不是自己 → 先停下，询问库主（用户）或等锁释放，**不要直接改**。
- **开始改前**：把自己的 `owner / since / intent` 写入 `lock`，并 `git commit`（"lock <工具>: ..."）。
- **改完后**：把 `lock` 置为 `null`，`status` 升级（beta→stable / 或加 `version`），再 `git commit`（"release <工具> vX: ..."）。

> 认领锁是软约定（不是强制锁文件），靠各 AI 自觉遵守。锁的持有时间应尽量短。

## 1.1 跨端协调：共享会话索引 `.sessions/`（已建）
仅靠 MANIFEST 软约定，两个 AI 若都跳过检查就会互相覆盖。因此引入 `AI工具库/.sessions/`：
- 每端 AI 一个文件 `<endpoint>__<ai>.json`，只写自己、只读别人 → 不冲突。
- 开工先 `python3 .sessions/sessionctl.py beat` 注册；改造工具前 `who-has --tool <名>` 查跨端锁，被锁则停下；`claim`/`release` 与 MANIFEST 锁双写双清。
- 详规见 [[SESSIONS]]（同目录 `SESSIONS.md`）。
- 协调文件由 verysync 同步、**不进 git**（`.gitignore` 已排除 `*.json` 易变状态）；规范与 `sessionctl.py` 入 git。

## 2. 版本与 MANIFEST
- 每次有实质改动，`version` 按语义化版本递增（bug 修 → patch，加功能 → minor，破坏性 → major）。
- `changelog` 数组记录每次改动（时间 + 改了啥 + 哪个 AI）。
- `status` 取值：`idea` / `beta` / `stable` / `deprecated`。

## 3. git 约定（本地仓库 + 双远程）
- 仓库范围：`AI工具库/` 自身（`.git` 已在 verysync 忽略列表内）。
- 提交信息格式：`<动作> <工具>: <一句话>`。动作：`add` / `lock` / `update` / `release` / `fix` / `docs`。
- 每个工具一次独立 commit，便于回溯。
- 双远程（gitea / github）**已接入**，push 规则与边界见 [[统一工具库接入指南]] 第6节「push 边界」（只 push 自己本次会话改造的工具，不替其他端/会话 commit 或 push 非本人改动）。

## 4. 跨 AI 可见性
- 工具代码与说明全部落在 vault 同步树内 → 任何一端、任何 AI 打开 vault 即见最新版。
- 改造记录同时写进 `MANIFEST.json changelog` 与 `git log` → 双保险可追。
- 涉及「新功能 / 新规则加入库」时，先给库主建议、确认后再写入（沿用 vault 既有护栏）。